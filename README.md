# WishShare

Приложение для создания и совместного использования вишлистов с поддержкой коллективных подарков и realtime‑обновлений.

## ✨ Функциональность

### Управление пользователями
- ✅ Регистрация по email/пароль
- ✅ Вход по email/пароль  
- ✅ Выход из аккаунта
- ✅ Изменение пароля
- ✅ Редактирование профиля (имя, аватар)
- ✅ OAuth Google и GitHub (полный flow с callback)

### Вишлисты
- ✅ Создание, редактирование, удаление вишлистов
- ✅ Уникальные публичные ссылки (slug)
- ✅ Три уровня приватности: PUBLIC, LINK_ONLY (по умолчанию), FRIENDS
- ✅ Secret Santa режим
- ✅ Просмотр списка личных вишлистов

### Подарки
- ✅ Создание, редактирование, удаление подарков
- ✅ Обычные подарки (бронирование целиком)
- ✅ Коллективные подарки (совместный сбор средств)
- ✅ Приватные подарки (видны только владельцу)
- ✅ Автозаполнение данных по URL (парсинг Open Graph)
- ✅ Прогресс-бар для коллективных подарков

### Резервирование и сбор средств
- ✅ Бронирование обычных подарков
- ✅ Вклады в коллективные подарки с правилами минимума/максимума
- ✅ Отмена резервов и вкладов
- ✅ Real-time синхронизация через WebSocket

## Архитектура

- **Frontend**: Next.js 16 (React 19), TypeScript, TanStack React Query, Zustand, Tailwind CSS, WebSocket
- **Backend**: FastAPI, SQLAlchemy (Async), SQLite/PostgreSQL, JWT, WebSocket
- **Аутентификация**: HttpOnly cookies + JWT токены
- **Realtime**: WebSocket для синхронизации вишлистов

## Быстрый старт

### Требования
- Python 3.11+
- Node.js 20.9+ (для Next.js 16)
- Redis (опционально, для production)

### 🚀 Быстрый запуск (рекомендуется)

Используйте единый файл запуска для автоматической настройки и запуска всех сервисов:

**Windows:**
```bash
start.bat
```

**Linux/Mac:**
```bash
chmod +x start.sh
./start.sh
```

**Кроссплатформенный (Python):**
```bash
python start.py
```

Скрипт автоматически:
- ✅ Проверит наличие Python и Node.js
- ✅ Создаст виртуальное окружение (если нужно)
- ✅ Установит зависимости бэкенда и фронтенда
- ✅ Установит браузеры Playwright
- ✅ Запустит оба сервиса

Backend будет доступен на `http://localhost:8000`  
Frontend будет доступен на `http://localhost:3000`

### Ручная установка и запуск

Если предпочитаете запускать вручную:

#### Backend

```bash
cd backend

# Создать виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt

# Установить браузеры Playwright (для парсинга URL)
playwright install chromium

# Настроить переменные окружения (опционально)
cp ../.env.example .env

# Запустить сервер
uvicorn app.main:app --reload
```

#### Frontend

```bash
cd frontend

# Установить зависимости
npm install

# Запустить dev сервер
npm run dev
```

## Конфигурация

### Переменные окружения

Скопируйте `.env.example` в `.env` и обновите значения:

**Для разработки (значения по умолчанию):**
- SQLite база данных (`./wishshare.db`)
- Redis не требуется
- CORS разрешен для локальной разработки (`localhost/127.0.0.1` на портах `3000` и `3001`)

**Для production:**
- Используйте PostgreSQL: `postgresql+asyncpg://user:pass@host:5432/db`
- Настройте Redis для кэширования
- Установите `JWT_SECRET_KEY` и другие secrets
- Добавьте корректные `BACKEND_CORS_ORIGINS`
- (Опционально) Настройте OAuth: `GOOGLE_CLIENT_ID`, `GITHUB_CLIENT_ID` и т.д.

## API Эндпоинты

### Аутентификация
```
POST   /auth/register              Регистрация
POST   /auth/login                 Вход
POST   /auth/logout                Выход
GET    /auth/me                    Текущий пользователь
PUT    /auth/me                    Редактирование профиля
POST   /auth/change-password       Смена пароля
POST   /auth/forgot-password       Запрос на сброс пароля
POST   /auth/reset-password        Подтверждение нового пароля по токену
GET    /auth/oauth/google          Получить URL для Google OAuth
GET    /auth/oauth/github          Получить URL для GitHub OAuth
GET    /auth/oauth/callback/google Callback Google OAuth
GET    /auth/oauth/callback/github Callback GitHub OAuth
```

### Вишлисты
```
GET    /wishlists                  Список личных вишлистов
GET    /wishlists/my               Список личных вишлистов
POST   /wishlists                  Создание вишлиста
GET    /wishlists/{slug}           Получение вишлиста
PUT    /wishlists/{slug}           Редактирование вишлиста
DELETE /wishlists/{slug}           Удаление вишлиста
```

### Подарки
```
POST   /wishlists/{slug}/gifts     Добавление подарка
PUT    /gifts/{gift_id}            Редактирование подарка
DELETE /gifts/{gift_id}            Удаление подарка
POST   /gifts/{gift_id}/reserve    Бронирование/внесение вклада
POST   /gifts/{gift_id}/cancel-reservation   Отмена резерва
POST   /gifts/{gift_id}/cancel-contribution  Отмена вклада
```

### Другое
```
POST   /parse-url                  Auto-fill товара по URL
POST   /og/preview                 Alias для обратной совместимости
WS     /ws/{slug}                  WebSocket для real-time обновлений
GET    /health                     Health check
```

## Структура проекта

```
WishShare/
├── backend/
│   ├── app/
│   │   ├── api/
│   │   │   ├── routes/
│   │   │   │   ├── auth.py       # Аутентификация и профиль
│   │   │   │   ├── wishlists.py  # Вишлисты и подарки (CRUD)
│   │   │   │   ├── og.py         # Auto-fill по URL
│   │   │   │   └── ws.py         # WebSocket
│   │   │   └── deps.py           # Зависимости (DB, current user)
│   │   ├── models/               # SQLAlchemy модели
│   │   ├── schemas/              # Pydantic схемы
│   │   ├── core/
│   │   │   ├── config.py         # Конфигурация
│   │   │   ├── security.py       # Хеширование, JWT
│   │   │   └── oauth.py          # OAuth helpers
│   │   ├── db/                   # Database setup
│   │   ├── realtime/             # WebSocket manager
│   │   └── main.py               # FastAPI app
│   ├── requirements.txt
│   ├── tests_smoke.py
│   └── .env                       # Переменные окружения (git ignored)
├── frontend/
│   ├── app/
│   │   ├── auth/                 # Login/Register страницы
│   │   ├── dashboard/            # Личный кабинет
│   │   ├── wishlist/             # Просмотр вишлиста
│   │   └── layout.tsx
│   ├── lib/
│   │   ├── api.ts                # API клиент
│   │   └── ws.ts                 # WebSocket подключение
│   ├── store/
│   │   └── auth.ts               # Zustand хранилище
│   ├── package.json
│   └── tsconfig.json
├── .env.example                  # Пример переменных окружения
└── README.md
```

## Тестирование

### Smoke-тесты

Скрипт [tests_smoke.py](./backend/tests_smoke.py) проверяет критические сценарии end-to-end:

```bash
cd backend
python tests_smoke.py
```

Проверяет:
1. Регистрацию пользователя
2. Вход и получение текущего пользователя
3. Создание вишлиста
4. Парсинг OpenGraph по URL
5. Создание и резервирование подарков
6. Отмену резерва

## Безопасность

- ✅ **XSS protection**: HttpOnly cookies, Content-Security-Policy
- ✅ **CSRF protection**: SameSite=Lax cookies, CORS
- ✅ **Валидация**: Pydantic на бэкенде, проверка прав доступа
- ✅ **Хеширование паролей**: bcrypt с солью
- ✅ **JWT токены**: HS256 подписание, HttpOnly cookies
- ✅ **Error handling**: Глобальный обработчик ошибок без утечки stack trace

## Развертывание

### Docker (рекомендуется для production)

```dockerfile
# backend/Dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY app/ ./app/
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

```dockerfile
# frontend/Dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
RUN npm run build
CMD ["npm", "start"]
```

### PostgreSQL для production

Вместо SQLite используйте PostgreSQL:

```bash
# Установка PostgreSQL
# macOS: brew install postgresql
# Ubuntu: sudo apt install postgresql
# Windows: https://www.postgresql.org/download/windows/

# Создание базы для приложения
createdb wishshare

# Обновление .env
POSTGRES_DSN=postgresql+asyncpg://postgres:password@localhost:5432/wishshare
```

## TODO / Планы развития

- [ ] Email уведомления (новые вклады, резервы)
- [ ] Система комментариев к подаркам
- [ ] Уведомления в реальном времени изнутри приложения
- [ ] Поддержка установки приложения (PWA)
- [ ] Мобильное приложение (React Native)
- [ ] Analytics и метрики использования
- [ ] Интеграция с платежными системами (для благотворительности)
- [ ] Сортировка и фильтрация вишлистов
- [ ] Импорт вишлистов из других источников

## Лицензия

MIT License - см. LICENSE файл для деталей

## Связь

Для вопросов, багов или предложений создавайте issues в репозитории.
