# Руководство по запуску WishShare

## 🚀 Быстрый старт

### Вариант 1: Единый файл запуска (рекомендуется)

Самый простой способ запустить проект - использовать единый файл запуска:

#### Windows:
```bash
start.bat
```

#### Linux/Mac:
```bash
chmod +x start.sh
./start.sh
```

#### Кроссплатформенный (Python):
```bash
python start.py
```

**Что делает скрипт:**
- ✅ Проверяет наличие Python и Node.js
- ✅ Создает виртуальное окружение для бэкенда (если нужно)
- ✅ Устанавливает зависимости бэкенда (`pip install -r requirements.txt`)
- ✅ Устанавливает браузеры Playwright для парсинга URL
- ✅ Устанавливает зависимости фронтенда (`npm install`)
- ✅ Запускает оба сервиса одновременно

**Результат:**
- Backend: http://localhost:8000
- Frontend: http://localhost:3000

### Вариант 2: Ручной запуск

Если нужно запускать сервисы отдельно или в разных терминалах:

#### Терминал 1 - Backend:
```bash
cd backend
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows

pip install -r requirements.txt
playwright install chromium
uvicorn app.main:app --reload
```

#### Терминал 2 - Frontend:
```bash
cd frontend
npm install
npm run dev
```

## 📋 Требования

Перед запуском убедитесь, что установлены:

- **Python 3.11+** - для бэкенда
- **Node.js 20.9+** - для фронтенда
- **npm** - обычно идет вместе с Node.js

### Проверка версий:

```bash
python --version  # или python3 --version
node --version
npm --version
```

## 🔧 Настройка

### Переменные окружения

Скопируйте `.env.example` в `.env` и настройте при необходимости:

```bash
cp .env.example backend/.env
```

Основные настройки уже имеют значения по умолчанию для разработки.

## 🐛 Решение проблем

### Проблема: "Python не найден"

**Решение:**
- Установите Python 3.11+ с https://www.python.org/
- Убедитесь, что Python добавлен в PATH

### Проблема: "Node.js не найден"

**Решение:**
- Установите Node.js 20.9+ с https://nodejs.org/
- Убедитесь, что Node.js добавлен в PATH

### Проблема: Ошибки при установке зависимостей

**Решение:**
- Обновите pip: `python -m pip install --upgrade pip`
- Обновите npm: `npm install -g npm@latest`
- Попробуйте удалить `node_modules` и `.venv` и переустановить

### Проблема: Порт уже занят

**Решение:**
- Backend (8000): Измените порт в команде запуска: `--port 8001`
- Frontend (3000): Создайте файл `frontend/.env.local` с `PORT=3001`

### Проблема: Playwright не работает

**Решение:**
```bash
cd backend
playwright install chromium
```

## 📝 Структура файлов запуска

- `start.bat` - скрипт для Windows
- `start.sh` - скрипт для Linux/Mac
- `start.py` - кроссплатформенный Python скрипт

Все скрипты делают одно и то же, выбирайте удобный для вашей системы.

## 🎯 Что дальше?

После успешного запуска:

1. Откройте браузер: http://localhost:3000
2. Зарегистрируйтесь или войдите
3. Создайте вишлист
4. Добавьте подарки с автозаполнением по URL

## 💡 Советы

- Используйте единый файл запуска для первого запуска
- Для разработки можно запускать сервисы отдельно в разных терминалах
- Проверяйте логи в консоли для отладки
- Backend логи показывают все запросы и ошибки
- Frontend логи показывают ошибки компиляции и runtime ошибки
