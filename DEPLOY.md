## 🚀 Деплой WishShare

Ниже — один из простых вариантов, остаёмся в рамках текущего ТЗ (FastAPI + Next.js).

---

### Вариант 1: Backend на Render, Frontend на Vercel (рекомендуется)

Полный чеклист: `DEPLOY_RENDER_VERCEL.md`.

#### 1. Подготовка репозитория
- Создайте репозиторий на GitHub, GitLab или Bitbucket.
- Добавьте в репозиторий папки `backend/` и `frontend/` (всё, что у вас сейчас локально).
- Закоммитьте и запушьте код.

```bash
git init
git add .
git commit -m "Initial WishShare project"
git remote add origin <URL_ВАШЕГО_REPO>
git push -u origin main
```

#### 2. Backend (FastAPI) на Render

1. Зайдите на `https://render.com` и залогиньтесь через GitHub.
2. Создайте **New +** → **Web Service**.
3. Выберите ваш репозиторий.
4. Настройки:
   - **Root Directory**: `backend`
   - **Environment**: `Python`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn app.main:app --host 0.0.0.0 --port 8000`
   - **Instance Type**: Free (для начала можно Free)
5. В разделе **Environment Variables**:
   - Скопируйте значения из вашего локального `.env.example` в переменные Render.
   - Обязательно задайте:
     - `POSTGRES_DSN` (если используете PostgreSQL на Render)
     - `JWT_SECRET_KEY` (случайная строка)
     - `BACKEND_URL` (можно не указывать, FastAPI сам вернёт URL)
6. Нажмите **Create Web Service** и дождитесь деплоя.

В итоге вы получите `https://<your-backend>.onrender.com`.

#### 3. Frontend (Next.js) на Vercel

1. Перейдите на `https://vercel.com` и залогиньтесь через GitHub.
2. Нажмите **New Project** → выберите ваш репозиторий.
3. В настройках проекта:
   - **Root Directory**: `frontend`
   - **Framework Preset**: `Next.js`
4. В **Environment Variables**:
   - `NEXT_PUBLIC_API_BASE_URL` = URL вашего backend на Render, например:

     `https://<your-backend>.onrender.com`
   - `NEXT_PUBLIC_WS_BASE_URL` = WebSocket URL backend, например:

     `wss://<your-backend>.onrender.com/ws`

5. Нажмите **Deploy**.

После деплоя у вас будет ссылка вида `https://your-wishshare-frontend.vercel.app`, которой можно делиться с друзьями.

---

### Вариант 2: Свой VPS + Docker (кратко)

Если хотите всё держать на одном сервере:

1. Возьмите VPS (Hetzner / DigitalOcean / любой провайдер).
2. Установите Docker + Docker Compose.
3. Создайте `docker-compose.yml` (упрощённый пример):

```yaml
version: "3.9"
services:
  backend:
    build: ./backend
    environment:
      - POSTGRES_DSN=postgresql+asyncpg://user:pass@db:5432/wishshare
      - JWT_SECRET_KEY=super-secret
      - BACKEND_URL=https://your-domain.com/api
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    environment:
      - NEXT_PUBLIC_API_BASE_URL=/api
      - NEXT_PUBLIC_WS_BASE_URL=/ws
    ports:
      - "3000:3000"

  db:
    image: postgres:16
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: wishshare
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
```

4. Настройте nginx/traefik и домен, чтобы:
   - `https://your-domain.com` → frontend (порт 3000)
   - `https://your-domain.com/api` → backend (порт 8000)
   - `https://your-domain.com/ws` → backend WebSocket (порт 8000)

---

### Почему сервис загружается долго?

Разделим на два кейса:

#### 1. Первый запуск локально через `start.bat`

При самом первом запуске скрипт делает тяжёлые операции:
- Создаёт виртуальное окружение Python
- Устанавливает все зависимости бэкенда (`pip install -r requirements.txt`)
- Устанавливает Playwright и качает Chromium (~140 MB)
- Устанавливает зависимости фронтенда (`npm install`)
- Next.js делает первый билд dev-сервера

Это *нормально* занимает:
- 3–10 минут на слабой машине
- 1–3 минуты на более мощной

Повторные запуски (когда venv и node_modules уже есть) должны занимать:
- ~5–15 секунд до Ready для бэкенда
- ~10–30 секунд до Ready для фронтенда (Next dev)

#### 2. Медленная загрузка сайта в браузере

Причины могут быть:
- Тяжёлый JS-бандл (Next dev-режим грузит много debug-кода)
- Отсутствие продакшн-сборки (нужен `next build` и `next start`)
- Первичная инициализация React Query / Zustand
- Подключение шрифтов и стилей

**Как ускорить:**
- Для продакшена обязательно использовать:

```bash
cd frontend
npm run build
npm start
```

- В dev-режиме считать, что это нормально, что всё чуть тяжелее.

---

### Резюме

- **Единая точка входа для деплоя**: Render (backend) + Vercel (frontend).
- **Локальный запуск**: `start.bat` (Windows) / `start.sh` (Linux/Mac) / `start.py`.
- **Долгая загрузка при первом запуске** — в основном из‑за установки зависимостей и Playwright; последующие запуски будут существенно быстрее.
